---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageNav from '../../components/PageNavigation.astro';
import { martialArtsRegistry } from '../../data/martial-arts/index.js';
import { renderStylePage } from '../../data/martial-arts/renderer.js';

export async function getStaticPaths() {
  return martialArtsRegistry.map(style => ({
    params: { style: style.id },
    props: { styleData: style }
  }));
}

const { styleData } = Astro.props;
const { html } = renderStylePage(styleData);
---

<BaseLayout title={`${styleData.name} Curriculum`} description={styleData.description}>
    <PageNav currentPage="martialarts" />

    <div class="min-h-screen bg-[#08080a]">
      <div class="text-[#a89e92] py-16 px-6" style="font-family: 'Courier New', Courier, monospace;">
        <div class="max-w-7xl mx-auto">
          
          <div class="mb-8 pb-6 border-b border-[#2a2a2e]">
            <a href="/martialarts" class="text-xs text-[#6a6260] hover:text-[#e0aa40] mb-4 inline-flex items-center gap-1 transition-colors">
              <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" /></svg>
              Back to Directory
            </a>
            
            <div class="flex items-start gap-4">
              <span class="text-4xl bg-[#0e0e10] p-3 rounded-lg border border-[#2a2a2e]">{styleData.icon}</span>
              <div>
                <h1 class="text-4xl font-serif text-white mb-2">{styleData.name}</h1>
                <p class="text-sm text-[#6a6260] mb-3 font-mono">{styleData.origin}</p>
                <p class="text-[#a89e92] text-sm leading-relaxed max-w-2xl">{styleData.description}</p>
              </div>
            </div>
            
            <div set:html={html.principles}></div>
          </div>

          <div set:html={html.toc}></div>

          <main class="space-y-16">
            <div set:html={html.techniques}></div>
            <div set:html={html.physical}></div>
            <div set:html={html.knowledge}></div>
            <div set:html={html.equipment}></div>
            <div set:html={html.drills}></div>
            <div set:html={html.tips}></div>
          </main>
        </div>
      </div>

      <div class="border-t border-[#2a2a2e]/50 py-6">
        <div class="max-w-7xl mx-auto px-6">
          <a href="/" class="flex items-center gap-2 text-[#6a6260] hover:text-[#e0aa40] transition-colors text-sm">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="19" y1="12" x2="5" y2="12" /><polyline points="12 19 5 12 12 5" />
            </svg>
            Back to Home
          </a>
        </div>
      </div>
    </div>

    <script>
      document.addEventListener('DOMContentLoaded', () => {
        document.querySelectorAll('.toc-link').forEach(link => {
          link.addEventListener('click', e => {
            e.preventDefault();
            const target = document.getElementById(link.getAttribute('href').substring(1));
            if (target) {
              target.scrollIntoView({ behavior: 'smooth', block: 'start' });
              history.pushState(null, null, `#${target.id}`);
            }
          });
        });

        const sections = document.querySelectorAll('section[id]');
        const links = document.querySelectorAll('.toc-link');
        const observer = new IntersectionObserver(entries => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              links.forEach(l => {
                l.style.backgroundColor = 'transparent';
                l.style.color = '#6a6260';
                l.style.borderColor = '#2a2a2e';
              });
              const active = document.querySelector(`.toc-link[href="#${entry.target.id}"]`);
              if (active) {
                active.style.backgroundColor = '#121214';
                active.style.color = '#e0aa40';
                active.style.borderColor = '#e0aa40';
              }
            }
          });
        }, { rootMargin: '-20% 0px -70% 0px' });

        sections.forEach(s => observer.observe(s));
      });
    </script>
</BaseLayout>